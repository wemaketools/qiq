{
  "id": "T-038",
  "title": "Global search: tenant-scoped grouped type-ahead endpoint",
  "status": "PASS",
  "iteration_count": 0,
  "max_iterations": 10,
  "depends_on": [
    "T-026",
    "T-023",
    "T-021"
  ],
  "objective": "Port GET /search: tenant-scoped grouped type-ahead across leads, quotes, parties, and brokers with the reference grouping/ranking/limit semantics feeding the existing topbar component (P-14).",
  "scope": [
    "GET /api/v1/search?q=... returning grouped results (leads/quotes/parties/brokers) with the reference DTO (type, id, display fields, route hints)",
    "Match semantics ported from the .NET implementation (ref numbers, names, external refs — verify exact fields and ranking)",
    "Tenant isolation; breadth permissions honored where the reference honors them; result limits per group"
  ],
  "out_of_scope": [
    "Topbar UI (exists; unchanged)",
    "Full-text-search infrastructure beyond what the reference uses"
  ],
  "implementation_details": [
    "Mirror the reference query approach (ILIKE/prefix patterns) — do not introduce new search infra; per-group limited UNION queries, tenant-predicated.",
    "Minimum query length + debounce contract per reference API behavior.",
    "Index review: ensure supporting indexes from T-005 cover the search paths."
  ],
  "files": [
    "src/server/domains/search/routes.ts",
    "src/server/domains/search/service.ts",
    "src/server/tests/integration/search.test.ts"
  ],
  "test_plan": {
    "unit": [
      "None (query behavior — integration level)"
    ],
    "integration": [
      "Grouped results across all four types from fixtures; ranking/limits per reference; below-min-length behavior",
      "Tenant isolation (no cross-tenant hits); permission/breadth behavior parity"
    ],
    "e2e": [
      "Topbar search spec adapted in T-043"
    ]
  },
  "acceptance_ids": [
    "AC-022",
    "AC-080"
  ],
  "verification_ids": [
    "V-027",
    "V-101"
  ],
  "findings": [],
  "history": [
    {
      "timestamp": "2026-07-18T17:00:00-04:00",
      "event": "created",
      "message": "Task generated by Planner from approved spec.md. Maps to P-14, section 12 session/platform search."
    },
    {
      "timestamp": "2026-07-20T18:12:00-04:00",
      "event": "implemented",
      "message": "Ported GET /api/v1/search from SearchStore.cs. Match columns/ranking/limit verified against BOTH the .NET DTO and the SPA topbar (searchApi.ts/GlobalSearch.tsx): leads match lead_ref/external_ref PREFIX + party-name SUBSTRING + pg_trgm similarity>=0.3, order lead_ref asc,id asc; quotes match quote_ref PREFIX ONLY, order quote_ref asc,id asc; parties/brokers match name substring OR similarity>=0.3, order name asc,id asc (brokers LEFT JOIN broker_type -> nullable tier). Per-group limit default 5, min query length 2 (trim), reference imposes no upper cap on limitPerType (recorded, not invented). Breadth: leads/quotes narrow by leads.view_all via the same lead_assignments EXISTS predicate as LeadStore; parties/brokers tenant-wide (A-14). Gated leads.view. Files: src/server/domains/search/{schemas,repository,service,routes,index}.ts; integration test src/server/tests/integration/search.test.ts (25 tests); wired search slot in lib/router/app.ts + both composition roots + auth-wiring.test.ts pin. Gates: search+auth-wiring 76 passing; typecheck 0; lint 0; repo-layout/lint-no-any/data-api-lockdown green; zero row residue verified by direct query. Mutation testing: 7 mutants (parties/leads/quotes tenant predicate drops, leads ranking reversal, parties limit drop, brokers group neutered, breadth predicate drop) each proven applied by sha256 delta, killed by a targeted test, restored byte-identical (hash e66aa3dc...9006a re-verified). Tenant-predicate mutants required convention-violating cross-tenant fixtures (tenant-B rows referencing tenant-A refs) because the reference_items inner joins incidentally filter tenant-consistent data; those pins added. No open findings."
    },
    {
      "timestamp": "2026-07-20T18:30:00-04:00",
      "event": "evaluated",
      "message": "PASS. Match semantics for all four groups verified first-hand line-by-line against SearchStore.cs/GlobalSearchQueryHandler.cs/GlobalSearchQuery.cs/GlobalSearchDto.cs: leads (lead_ref/external_ref PREFIX OR party-name SUBSTRING OR similarity>=0.3), quotes (quote_ref PREFIX only), parties/brokers (substring OR similarity>=0.3), ordering, default limit 5, min length 2, no upper cap on limitPerType, breadth via lead_assignments EXISTS for leads/quotes only, parties/brokers tenant-wide -- all confirmed byte-accurate, no discrepancies found. DTO field names cross-checked against src/ui/src/features/search/searchApi.ts and GlobalSearch.tsx -- exact match, {leads,quotes,parties,brokers} with the pinned field names. Ran search.test.ts first-hand: 25/25 pass. Reproduced the parties-tenant-predicate mutant (dropped `pt.tenant_id = tenantId`, replaced with `1=1`): the cross-tenant pin test failed exactly as claimed (expected [125] not to include 125); restored the file. First restore attempt via Python text-mode write silently converted the file's original LF-only line endings to CRLF (sha 2384c5a1... instead of e66aa3dc...) -- caught by hash comparison, corrected with a binary CRLF->LF normalization pass, verified byte-identical to the recorded baseline sha256 e66aa3dc2b2875b5c7511ce0d0cfeb11c86ccbaa9865360386b98d6231d9006a. Confirmed zero NUL bytes. Confirmed zero DB residue directly: `select count(*) from tenants where name like 't038%'` = 0 after the suite. Confirmed ORDER BY is table-qualified in repository.ts (l.lead_ref, qu.quote_ref, pt.name, b.name -- no alias shadowing). Confirmed the two vacuous-fixture fixes are genuinely closed: (a) the convention-violating cross-tenant fixtures (crossPartyB/crossLeadB/crossQuoteB) exist and their three pinning tests pass, reproduced independently above; (b) the query terms are bare stems (Acme/Zeta/Brokers/Solobrand) without the shared RUN token, and each group's fixtures include both a hit and a non-hit, verified non-vacuous by inspection and by the passing per-group assertions. Confirmed npm run typecheck is 0 (F-042-3, the cross-task typecheck failure the sibling T-042 evaluation flagged, is resolved -- api/v1/[[...segments]].ts now imports and uses defaultSearchDeps). Confirmed npm run lint is 0. No blocking findings. Evaluated by security-review-task and evaluate-tests-code checklists concurrently: no missing tenant/permission checks found, requirePermission('leads.view') gate confirmed via the 403-for-outsider test, no client-suppliable breadth flag (server-resolved via resolveAccess), no SQL injection risk (all values parameterized via kysely sql tag), no sensitive data logged."
    }
  ]
}